import { createContext } from "react";

export const WkasContext = createContext(null);

export const GraphIsZoomedContext = createContext(null);

export const GraphsIndexContext = createContext(null);

export const MapInfoContext = createContext(null);

export const BaudauerContext = createContext(null);

export const FilterContext = createContext(null);
